:root {
  --pink: #f6c1cc;
  --light-gray: #e5e7eb;
  --dark-gray: #1f2933;
  --sage: #b7c9b4;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
  background: var(--light-gray);
  color: var(--dark-gray);
}

nav {
  background: white;
  padding: 1rem 2rem;
}

nav ul {
  list-style: none;
  display: flex;
  gap: 1.5rem;
  padding: 0;
  margin: 0;
}

nav a {
  text-decoration: none;
  color: var(--dark-gray);
  font-weight: bold;
}

header {
  text-align: center;
  padding: 4rem 1rem;
  background: linear-gradient(135deg, var(--sage), var(--pink));
}

header h1 {
  margin-bottom: 1rem;
}

.button, button {
  display: inline-block;
  background: var(--dark-gray);
  color: white;
  padding: 0.8rem 2rem;
  border-radius: 999px;
  text-decoration: none;
  border: none;
  cursor: pointer;
}

.button:hover, button:hover {
  background: var(--pink);
  color: var(--dark-gray);
}

main {
  max-width: 1100px;
  margin: auto;
  padding: 2rem;
}

.product-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
}

.product-card, .product-section, .content {
  background: white;
  padding: 2rem;
  border-radius: 16px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.1);
  margin-bottom: 2rem;
}

footer {
  text-align: center;
  padding: 2rem 1rem;
  background: var(--dark-gray);
  color: white;
}
